DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DE BANCO DE DADOS (NeonDB - PostgreSQL)
# -----------------------------------------------------------------------------
# Formato da Connection String:
# postgresql://[USUÁRIO]:[SENHA]@[HOST]/[DATABASE]?sslmode=require

# Steps para conexão externa (ex: DBeaver):
# 1. Host: ep-old-sky-acjs62gb.sa-east-1.aws.neon.tech
# 2. User: neondb_owner
# 3. Port: 5432 (padrão Postgres)
# 4. SSL: Required (sslmode=require)
# -----------------------------------------------------------------------------
